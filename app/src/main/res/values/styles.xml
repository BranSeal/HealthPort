<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="HealthPort" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/superLightGreyButtonColor</item>
        <item name="colorAccent">@color/lightGreyAccent</item>
    </style>

    <style name="AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="EditTextTheme">
        <item name="colorControlNormal">@color/hint_color</item>
        <item name="android:colorControlActivated">@color/text_color</item>
        <item name="android:colorControlHighlight">@color/text_color</item>
    </style>

    <style name="EditTextHoverHint" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/text_color</item>
        <item name="android:textSize">12sp</item>
    </style>

</resources>
